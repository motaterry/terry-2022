// UI Constants
// Centralized timing and delay values for consistent behavior

export const UI_CONSTANTS = {
  // Focus and blur delays (in milliseconds)
  BLUR_DELAY_MS: 200,
  FOCUS_DELAY_MS: 100,
  
  // Animation durations (in milliseconds)
  TRANSITION_DURATION_MS: 150,
  
  // Autocomplete configuration
  AUTCOMPLETE_MAX_HEIGHT: 240, // max-h-60 = 15rem = 240px
} as const

